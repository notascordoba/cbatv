services:
- type: web
  name: periodismo-bot
  env: python
  buildCommand: pip install -r requirements.txt
  startCommand: python app.py
  plan: free
  envVars:
  - key: PYTHON_VERSION
    value: 3.11.0
  - key: PORT
    value: 10000
